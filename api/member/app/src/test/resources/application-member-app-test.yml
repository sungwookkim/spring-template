spring:
  datasource:
    h2:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:mem:testdb
      username: sa
      password:
  jpa:
    open-in-view: false

decorator:
  datasource:
    p6spy:
      enable-logging: true

logging:
  level:
    org:
      springframework:
        transaction: trace