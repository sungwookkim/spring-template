spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/exampledb?useSSL=false&serverTimezone=Asia/Seoul
    username: user
    password: userpassword
  jpa:
    open-in-view: false


decorator:
  datasource:
    p6spy:
      enable-logging: true

logging:
  level:
    org:
      springframework:
        transaction: trace